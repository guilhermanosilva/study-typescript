{
  const message: string = "Hello world, Guilhermano";
  console.log(message);
}

/**
 * Para compilar basta entrar na pasta helloworld e executar o comando:
 * tsc ./app.ts
 */

/**
 * Para não precisar ficar compilando, vamos criar o arquivo tscconfig com o comando:
 * tsc --init
 * Execute o comando na raiz do projeto
 */
